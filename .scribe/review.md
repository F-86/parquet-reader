# AGENTS.md 诊断结果

## 文档类型

- 类型：`AGENTS.md`
- 目标受众：AI agent / 编程助手
- 修改模式：重构

## 🔴 严重（与 AGENTS.md 规范冲突）

- [ ] 标题仍是 `# AGETNS.md`，与实际文件名不一致。
  - 建议：改为项目名或 `# AGENTS.md`，避免 agent 误判上下文。

- [ ] 缺少项目概述。
  - 建议：用 2-3 句话说明这是 Rust 编写的 Parquet TUI Viewer，Python 原型只是行为参考。

- [ ] 缺少技术栈与核心技术方向。
  - 建议：说明 Rust、TUI、Parquet 读取、分页查询、Python 原型参考关系；避免写精确依赖版本。

- [ ] 缺少常用命令占位。
  - 建议：加入 `cargo fmt`、`cargo check`、`cargo test` 等高频命令；这些是 agent 最常需要执行的信息。

## 🟡 改进（偏离最佳实践）

- [ ] 当前内容是一组未分类规则，agent 执行时不易快速定位。
  - 建议：按「项目定位」「常用命令」「架构边界」「实现规则」「文档规则」「禁止行为」重排。

- [ ] 部分规则语义重复，可合并。
  - 建议：把“大文件保守假设”“默认分页”“不得全量加载”归入数据读取规则。

- [ ] 缺少明确的验证要求。
  - 建议：把每次改动的验证顺序写成具体命令优先级，而不是只说“通过格式化、编译或测试”。

- [ ] 缺少对 Python 原型的使用边界。
  - 建议：明确 `parquet_tui.py` 只能作为交互行为参考，不作为 Rust 架构照搬对象。

## 🟢 建议（锦上添花）

- [ ] 可增加“不要写可检索事实”的元规则，保留用户最初偏好。
  - 建议：放在「文档维护」章节，而不是散在规则列表末尾。

- [ ] 可增加“依赖选择需说明理由”的规则。
  - 建议：保留原规则，但归入实现规则。

## 修复日志

- 修正标题为项目名 `parquet-reader`。
- 增加项目定位和 Python 原型使用边界。
- 增加常用验证命令。
- 将原规则重排为架构边界、数据读取、TUI 交互、值显示与错误处理、依赖约束、文档维护、禁止行为。
- 合并重复规则，保留“不要写可检索事实”的要求。
